// Single source of truth for the running application version.
//
// The installed Self-Hosted app reports this in every heartbeat, so the
// Customer Portal can confirm the installation is alive and tell the customer
// whether it is behind the current release.
export const APP_VERSION = "1.0.0";
