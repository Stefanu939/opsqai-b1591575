# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Stray Windows-style paths created if a bootstrap/service script is run on a
# non-Windows machine (%ProgramData% / %ProgramFiles% are unset, so paths fall
# back to literal "C:\..." directory names). Committing these breaks
# `git checkout` on Windows runners with: invalid path 'C:\ProgramData/...'.
C:\\*
OPSQAI/
